# Example: AI App

**Stub.** This directory will demonstrate omnix installed in a representative LLM-powered application.

## Intended demonstration

- Python 3.12 (FastAPI) or Node (Hono).
- AI SDK / direct provider client.
- RAG over a small corpus (pgvector).
- Eval harness with golden examples.
- Prompt versioning in `prompts/`.

## omnix pieces shown

- `AGENTS.md` for any AI coding tool, plus `.cursor/rules/` if using Cursor.
- `.obsidian-ai-memory/08-PROMPTS/effective-prompts.md` populated.
- `09-AGENTS/agent-behavior-notes.md` capturing model preferences for this codebase.
- Eval regressions logged in `03-ERRORS/error-memory.md`.

## Status

Not yet populated. Tracking in `ROADMAP.md` (v0.5).
