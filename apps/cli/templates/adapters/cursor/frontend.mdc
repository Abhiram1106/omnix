---
description: Frontend rules for UI components, pages, and client-side code
globs:
  - "**/*.tsx"
  - "**/*.jsx"
  - "**/*.css"
  - "**/*.module.css"
  - "app/**"
  - "src/components/**"
  - "src/pages/**"
  - "apps/web/**"
  - "packages/ui/**"
alwaysApply: false
---

# Frontend Rules — Omnix Runtime

Source: `packages/core/standards/frontend.md`

- TypeScript strict. No `any`.
- Tailwind + design tokens. No inline styles for non-trivial UI.
- shadcn/ui or Radix primitives. Do not reinvent.
- Server/client component split is intentional — not accidental.
- Every interactive element is keyboard-reachable and announces correctly.
- Test with Testing Library; query by role.
- Measure before optimizing.
- No layout shift: reserve space for images, async content, fonts.
- Co-locate component files: `Component.tsx`, `Component.test.tsx`, `Component.module.css`.
- No props drilling past depth 2 — use context or state library.
- `useEffect` dependencies must be exhaustive.

Load `.cursor/context/frontend-context.md` for component library and design system conventions.
