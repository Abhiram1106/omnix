# Anti-Patterns

> Prevention rules promoted from `error-memory.md`.
> Append only — never edit existing entries.
> Read this file before any debugging session.

---

## How to use this file

When a bug in `error-memory.md` matches a pattern that has occurred more than once, or is severe enough that it should never happen again, promote it here as a "never do X" rule.

Format:

```
## <Anti-pattern title>
- Rule: Never / Always / Do not
- Why: what happens when the rule is violated
- From: link to error-memory.md entry that triggered this promotion
- Date promoted: YYYY-MM-DD
```

---

## Anti-patterns

<!-- Append new entries below this line. Never edit above entries. -->

