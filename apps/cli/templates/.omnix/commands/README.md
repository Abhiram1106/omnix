# .omnix/commands/

Project-specific CLI commands. Add `.md` spec files here to define custom
`omnix` commands that only apply to this project.

Example: `deploy-staging.md` describing a custom `omnix deploy-staging` command
that wraps your project's specific staging deploy steps.
