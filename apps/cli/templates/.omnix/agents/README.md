# .omnix/agents/

Project-specific agent overrides. Place `.md` files here to extend or override
default Omnix agent definitions from `packages/core/agents/`.

Example: create `frontend.md` to add project-specific frontend conventions
that override the default frontend agent for this project only.
