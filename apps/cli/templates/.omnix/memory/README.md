# .omnix/memory/

Active-session memory cache. Auto-managed by the Omnix Runtime.

**Gitignore this directory** — it contains session-scoped state that
should not be committed. Long-term memory lives in `.obsidian-ai-memory/`.
